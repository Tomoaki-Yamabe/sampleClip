# Proxy設定（必要に応じて設定）
# HTTP_PROXY=http://username:password@proxy-server:port
# HTTPS_PROXY=http://username:password@proxy-server:port
# NO_PROXY=localhost,127.0.0.1

# AWS設定
# AWS_REGION=ap-northeast-1
# AWS_ACCOUNT_ID=your-account-id

# Docker Build時のProxy設定例
# docker build \
#   --build-arg HTTP_PROXY=http://username:password@proxy-server:port \
#   --build-arg HTTPS_PROXY=http://username:password@proxy-server:port \
#   -t your-image-name .
